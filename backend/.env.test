# Test-only fixture, committed on purpose - contains no real secrets (a fixed dummy JWT
# value and a local sqlite path), unlike .env/.env.local which are always gitignored.
DEPLOYMENT_MODE=self_hosted
PORT=4111
DATABASE_URL="file:./test.db"
JWT_SECRET=test-only-jwt-secret-do-not-use-in-production-0000000000
